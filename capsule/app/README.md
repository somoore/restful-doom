# Capsule app data

The agent capsule fetches the shareware `DOOM1.WAD` at image build time when this directory is
empty. To override it, place an IWAD here before building the capsule.

WAD files are ignored by git.
