# Agent Brain Notes

## Milestones

- Started structured local brain implementation: fast protobuf-state policy loop, persistent map/enemy memory, reward-scored episode summaries, and simple parameter evolution.
- Added Codex MCP surface for `brain_drive` and `brain_memory` so future runs can be controlled through the local tool bridge instead of screenshots.
- Added Docker-backed MCP trainer design: restart Doom for each candidate, run a fresh protobuf rollout, score with rewards, and promote policy parameters only when they beat stored memory.
- First live Docker training batch completed: three fresh candidates ran and memory was written, but the policy got stuck and overused `ACTION_USE`; no kills or pickups yet.
- Updated the policy with a multi-phase stuck recovery sequence and fitness credit for explored cells and visible enemy ticks so evolution has useful signal before first kill.
- Second live Docker training batch showed improvement: candidates picked up an item, visited more cells, had far fewer stuck events, and promoted better parameters. Still no kills.
- Found a memory bug: enemy `last_seen_tick` from prior fresh episodes could look newer than the current episode tick, causing stale enemy chasing. Fixed by ignoring remembered enemies with future ticks.

## Current Hypothesis

The first useful intelligence should be a deterministic skill-selection policy backed by memory, not an LLM driving individual tics. The policy should learn enough from real rollouts to avoid repeating obvious failures, then later become a training target for a small learned model.

## Good State Definition

Training reached the first "good" state on E1M1: autonomously complete one level traversal and score at least one kill in that same run. The current verified run is `brain-5b63907febc4` from `brain-train-68-current-success.jsonl`: level transition to E1M2, 6 peak kills, 3 peak items, and 60 end health. The next target is repeatability and learned skill selection beyond behavior cloning.

## Export / Resume Contract

- Training job bundle schema: `restfuldoom.training_job.v1`.
- Bundle contents: manifest, `agent_memory/e1m1.json`, `agent-notes.md`, learned skill model checkpoints, and referenced trajectory JSONL files.
- Resume path: import the bundle into a cloud worker or Hellbox/Shrink job, then continue structured-brain episodes against the new gRPC endpoint using the promoted parameters from memory.

## Latest Milestones

- Third live Docker training batch improved further: best fitness reached `-11.42`, best candidate picked up 2 items, visited 29 cells, and reduced stuck events to 61. Still no kills and no level completion, so the training target is not met.
- Added explicit success criteria to the brain: success requires at least one kill and level completion by default.
- Added portable export/import for training jobs so progress can move from Docker to cloud.
- Added protobuf-driven `hunt_known_enemy`: the brain now uses all streamed living enemy coordinates, not screenshots or REST door lists, to close distance toward combat. Fitness now includes nearest-enemy distance progress.
- Fourth live Docker training batch showed known-enemy hunting reduced distance by about 435 units but got pinned against geometry with no line of sight. Added blocked-target memory so the brain stops chasing a known enemy through the same wall from the same cell.
- Added engine-side protobuf navigation probes instead of using screenshots or REST door lists: `forward_open`, side/back openness, `use_line_ahead`, and front blocking distance/special. The brain now uses those probes to use doors, sidestep, or turn when the direct route is blocked.
- Added engine-side combat probes and ray-fan navigation. Early batches confirmed the agent could see and approach enemies from structured state, but it still stayed outside pistol range until short-term contact-corridor memory kept it moving through the opening after line of sight dropped.
- First real combat breakthrough: live Docker batch `brain-train-15-directional-use` candidate 3 dealt 40 enemy damage, scored 3 kills, reached 237 visible-enemy ticks, and promoted run `brain-86b41d87a2f7` with score `386.6738`. Not a good state yet because the level was not completed.
- Next hypothesis: use nearby special/use-line data from protobuf to make door/switch/exit interactions explicit enough for the promoted combat policy to finish E1M1 rather than only clearing enemies.
- Longer promoted-policy run `brain-train-16-use-lines-long` candidate 1 improved again: 45 enemy damage, 3 kills, 8 items, 80 visited cells, max position about `x=2480, y=-2128`, and promoted run `brain-eea454042350` with score `417.3823`. Still no level completion by 8,000 states.
- Found that using special-line midpoint distance causes bad door/switch behavior on long linedefs. Added nearest-point-on-line data to protobuf/FFI so the brain can use actual segment distance and angle.
- Added bounded `seek_known_enemy`, manual-use special filtering, line-attempt blacklisting, and route-to-progression movement over the protobuf direction-probe fan. This moved the agent from wandering into reliable first-contact combat and post-kill progression attempts.
- Best run so far: `brain-train-25-exit-assist` / `brain-329df98b66bf` promoted with score `419.3735`: 6 kills, 2 items, 95 visited cells, alive at max state, and reached the exit approach around `y=-4641`. Not a good state yet because the map did not transition.
- Regression found: applying front-side line targeting to all manual specials made the policy worse and caused an early death. Narrowed side-aware behavior to local exit switches only; ordinary doors now use the earlier successful behavior.
- New best rollout `brain-train-31-critical-retreat` / `brain-c560bed0a254` promoted with score `436.1415`: 6 kills, 2 items, alive with 60 health at 16,000 states, but still no level transition. The final blocker was pressing line 330 from about 168 map units away.
- Added learned hazard-cell handling after repeated deaths on damaging floor cells around line 195. The policy now records non-combat health loss in memory and has explicit hazard escape skills. First hazard escape prevented the immediate floor death, but over-broad hazard routing distorted the path; narrowed it toward progression lines and close-walk-trigger skipping.
- Added critical-health defensive fire after a late death against enemy id 9 showed the agent retreating passively while aligned at close range. The policy now shoots while backing/strafe at critical health.
- Current exit-door evidence: line 330 is visible and selected in cell `23:-36`, but cannot be activated from ~168 units. Line 325 is a close front-side manual door and should be aligned/used before returning to line 330. Added tests for close angled assist-door turn/use and stuck manual-line use around door lines 340/341.
- Strongest failed rollouts now reliably reach the final exit room: candidates 58, 60, 61, and 62 cleared the map enemies with 6 kills but stalled at line 330. The current blocker is retrying close assist doors 324/325 after line-attempt blacklisting; added an exit-specific retry path that can press line 325 with a small forward nudge once the exit push stalls.
- Candidate 65 exposed a separate mid-map regression: after 2 kills the policy chased far walk trigger line 308 from ~1900 units away and stopped hunting the remaining monsters. Tightened progression readiness so distant walk triggers wait until more enemies are cleared, while known-enemy seeking remains active until roughly the 5-kill route.
- Candidate 66 achieved the first good state: autonomous E1M1 completion with 6 kills on the route, assist-door retries, and a final line 330 activation into E1M2. Candidate 67 then exposed a close-exit ordering bug where line 330 at distance 16/angle 0 still routed instead of pressing; moved `press_exit_switch` ahead of blocked-front routing and fixed stats to preserve peak kills/items across map-reset counters.
- Candidate 68 verified the current code after that fix: `success=true`, `level_completed=true`, `kill_delta=6`, `peak_kills=6`, `item_delta=3`, `peak_items=3`, and `press_exit_switch=2`. Trained the first learned skill selector from this trajectory: `agent_models/skill-policy.json`, schema `restfuldoom.skill_policy.v1`, 2,961 samples, 29 classes, train accuracy `0.8982`, eval accuracy `0.882`.
- Started the full PPO transition. This is not learning evidence yet: the existing learned selector is behavior cloning, so the next milestone is fast resettable episodes plus a Gym-style protobuf environment that lets a PPO policy choose high-level skills from reward feedback.
- Added the first fast-reset control path: protobuf `ResetEpisode`, Rust gRPC queue, fixed-size FFI control request, and C-side polling before `G_Ticker`. Rust tests pass for request normalization and gRPC-to-FFI reset delivery. Seed is explicitly reported as not yet applied, so deterministic seeding is still an open PPO repeatability risk.
- Added the first Gym-style PPO layer: `DoomAgentEnv.reset()`, `DoomAgentEnv.step(action)`, compact observation vectors using the existing protobuf feature schema, eight high-level PPO skill actions, rollout buffer JSONL export, PPO trainer/checkpoint code, and a promotion gate. Installed Torch/NumPy in the agent venv and verified the real PPO update plus checkpoint roundtrip in tests.
- First live PPO smoke against rebuilt Docker gRPC Doom succeeded: `ppo-smoke-local` collected 32 real transitions, ran one PPO update, wrote `trajectories/ppo/ppo-smoke-local-buffer-0000.jsonl`, saved `agent_models/ppo/ppo-smoke-local-ppo-0000.pt`, recorded it in memory, and exported/imported a resume bundle containing the PPO checkpoint. This was not learning-quality evidence: the short rollout had no kills and no positive reward.
- Added dense combat feedback to the reward preset path: `damage_delta` now comes from protobuf enemy health changes and combat PPO gets reward before a full kill. This should make early PPO less sparse than kill-only feedback.
- Second live PPO smoke (`ppo-damage-smoke`) collected 256 real transitions but still produced zero reward because it never damaged an enemy. Added nearest-enemy distance progress from protobuf enemy distances to make combat PPO receive earlier navigation feedback before line of sight or damage.
- First continuous PPO trend run (`ppo-learning-smoke`) ran 3 updates of 256 real transitions each. Rewards were `2.4292`, `-28.981` with one death, then `3.3805`; nearest-enemy progress was positive in all three updates, but no kills or damage yet. This is a real reward-feedback signal, not promotion-worthy learning.
- Added macro-step PPO semantics so one PPO skill selection waits through the commanded action duration before scoring a transition. Live run `ppo-macro-smoke` improved dense reward from `6.181` to `9.5463` over three real updates and increased nearest-enemy progress, but still produced `damage_delta=0` and `max_kills=0`.
- Found and fixed a promotion-gate bug: shaping reward alone could mark a zero-kill, zero-completion checkpoint promotable. The gate now requires the success floor by default: level completion rate `>= 1.0` and mean kills `>= 1.0`, plus baseline comparisons.
- Added action-aware combat shaping for PPO: firing when the protobuf combat probe reports a shootable enemy earns reward, missing that opportunity is penalized, and blind firing is lightly penalized. Live run `ppo-combat-shaping-smoke` improved dense reward to `11.0886`, but reported `shootable_target_steps=0`, `damage_delta=0`, and `max_kills=0`. Bad milestone: PPO is learning approach behavior, but not yet reaching valid firing affordances.
- Added `docs/agent-learning-architecture.md` to make the learning stack explicit: fast controller vs decision layer, memory schema, skill representation, PPO observation contract, promotion rule, and current observation gaps.
- Added behavior-cloning warm start for PPO from successful protobuf trajectories. Run `ppo-bc-live-smoke` trained on 2,964 expert samples with `bc_accuracy=0.839`, then ran five live PPO updates. Bad milestone: the policy still got `shootable_target_steps=0`, `damage_delta=0`, and `max_kills=0`, and eval blocked promotion against the heuristic baseline. This points back to observation/action abstraction, not just optimizer initialization.
- Added map position and facing to the PPO observation vector. Run `ppo-pos-bc-live-smoke` retrained BC with the richer vector (`bc_accuracy=0.835`) and ran five live PPO updates. Bad milestone: still `shootable_target_steps=0`, `damage_delta=0`, and `max_kills=0`; position helps route distinguishability but does not by itself create combat starts.
- Added bounded reset curriculum warmup with visible/shootable stop modes and warmup metrics. Naive shootable and visible warmups from spawn were too slow for the PPO inner loop and had to be interrupted. Bounded smoke `ppo-bounded-curriculum-metric-smoke` correctly reported one warmup of `350` tics stopped by `tic_limit`, but still reached no visible/shootable combat. Next likely unlock is cached combat starts / server-side snapshot restore, not more reward tweaking.
- Added server-side `EpisodeStart` curriculum starts to `ResetEpisode`: the client can request a starting position, face the nearest enemy, and apply health/ammo resources after a fresh level reset. Docker validation confirmed exact reset placement and resource application through protobuf state.
- Fresh combat-start search found a legal E1M1 curriculum point at `x=3248, y=-3280` Doom units (`x_fp=212860928`, `y_fp=-214958080`) with line of sight to enemies 5, 6, and 7 and an immediate shootable target. This is materially better for PPO than replaying 500+ route tics from spawn for every episode.
- Tightened the learning contract: exported `restfuldoom.observation.v1` now includes feature descriptors, base protobuf/memory features, and 10 macro-action history features; exported `restfuldoom.skill_action.v1` now carries machine-readable definitions for each PPO skill. `AgentMemory.remembered_enemies()` is now the explicit query path used by feature extraction.
- BC-warm PPO combat-start smoke `ppo-episode-start-combat-smoke` produced the first PPO-layer kill evidence from a fresh reset start: three updates of 128 transitions each all reached `max_kills=1`, with `damage_delta` 60, 65, and 70, and `shootable_target_steps` 113, 115, and 111. This is strong evidence that the PPO environment/reward/controller loop can score combat, but it is not pure independent learning because the actor was behavior-cloned first.
- From-scratch PPO combat-start smoke `ppo-independent-combat-start-smoke` showed independent reward-driven improvement but not kills yet. Fire selections increased from 15/128 to 43/128, action reward from `1.91` to `11.73`, total reward from `12.4953` to `23.2953`, and late updates recovered `damage_delta=40`; `max_kills` stayed 0 across eight short updates. This is the first useful PPO-from-scratch learning signal, but not promotion-worthy.
- Added PPO action masking from protobuf/memory affordances. The first broad mask removed impossible actions but still let the policy overuse retreat/seek loops during shootable combat and got no kills. Tightening the mask so visible combat mostly chooses between `engage` and `fire` produced the first pure PPO combat success.
- From-scratch masked PPO run `ppo-tight-mask-independent-combat-start-smoke` reached `max_kills>=1` in every one of eight 128-transition updates, with zero invalid actions. Fire selections rose from 49 to 82 by update 4, and update 7 reached `max_kills=2`, `damage_delta=60`, and `total_reward=60.3412`. This is real independent PPO learning over high-level skills, not behavior cloning.
- Deterministic eval of checkpoint `agent_models/ppo/ppo-tight-mask-independent-combat-start-smoke-ppo-0007.pt` on three combat-start episodes repeated one kill in all three episodes and beat masked-random on reward (`70.8597` vs `41.7365`) with survival `1.0` and zero recover-stuck events. This is a combat-curriculum promotion only; it does not prove full-level navigation competence yet.
- Added PPO checkpoint resume so a learned actor can continue from an exported `.pt` instead of restarting from random weights or BC. Transfer smoke `ppo-transfer-spawn-smoke` resumed the combat checkpoint from normal E1M1 spawn and ran two 256-transition updates. Bad milestone: it reduced nearest-enemy distance by about 650 units using `route_progression`/`seek_enemy`, but still got `shootable_target_steps=0`, `damage_delta=0`, and `max_kills=0`. The next gap is spawn-to-first-contact curriculum, not combat execution.
- Tightened the architecture contracts after review feedback: exported `restfuldoom.decision_cycle.v1` now describes the fast-controller/decision-layer handshake, `restfuldoom.agent_memory_contract.v1` describes concrete memory query/update paths, skill definitions explicitly mark the actions as code-defined options with PPO learning the selection policy, and rollout/checkpoint metadata now carries these contracts. This does not add new learning evidence, but it makes future training artifacts auditable.
- Added current-sector hazard and route-waypoint protobuf observations. The C bridge now publishes sector id/special/floor/ceiling/light, floor-damage flags, and a cheap progression waypoint; Python PPO features expanded from 52 to 64 features. Older PPO checkpoints can resume through observation expansion: existing input weights are preserved and new feature columns start at zero. This is architecture progress, not new learning evidence yet.
- Added route-outcome macro history for PPO. The environment now computes distance gained/lost toward the previous route waypoint, reached/failed flags, failed route-attempt streak, route reward shaping, and rollout metadata. PPO observations expanded from 64 to 69 features. Focused Python tests pass. Live Docker probe confirmed 69 streamed features plus route outcome metadata, and tiny PPO run `ppo-route-history-probe` completed one 16-record update from spawn with route/seek choices and no invalid actions. Bad milestone: it still reached no shootable target, damage, or kill, so spawn-to-contact PPO remains unsolved.
- Initially added named PPO reset-start curriculum `e1m1-spawn-to-combat` with `restfuldoom.ppo_curriculum.v1` metadata. The first version rotated combat start, first-visible trajectory coordinates, opening-route trajectory coordinates, and true spawn. Live smoke `ppo-curriculum-probe` resumed the independent combat checkpoint and completed two 16-record updates without Docker restart: `combat_start` produced 16 shootable-target steps and 15 damage, while `first_visible_enemy` produced positive distance progress but still no shootable target or kill. This validated curriculum plumbing but exposed first-visible-to-combat transfer as a learning gap.
- Rechecked the curriculum with live fresh resets. Bad milestone: trajectory-derived coordinates for first-visible/opening-route do not recreate progressed-map context after `ResetEpisode`; they lose opened doors/enemy movement and do not bridge to shootable combat. Replaced the named curriculum stages with validated fresh-reset combat variants plus true spawn, and added route summary fields (`route_attempt_steps`, `route_failed_steps`, `route_reached_steps`, `route_progress_units`, `route_action_reward`) so spawn-to-contact progress is visible without hand-parsing JSONL.
- Longer spawn-only PPO trend `ppo-spawn-trend` resumed the independent combat checkpoint for six 128-transition updates from real E1M1 spawn. Good signal: every buffer made positive route progress/reward. Bad signal: all six buffers still had `shootable_target_steps=0`, `damage_delta=0`, and `max_kills=0`. Current conclusion: combat-start PPO is real, but full-level PPO needs either true snapshot/progressed-state curriculum or a richer route/topology/temporal observation before it can reliably find first contact from spawn.
- Added bounded temporal-context PPO features. Observations expanded from 69 to 80 features by appending movement delta, enemy-distance trend, route-distance trend, same-cell streak, recent visible/shootable contact, and rolling route progress/failure signals. This preserves old checkpoint prefixes for migration while giving feed-forward PPO more than a single static frame. Live smoke `ppo-temporal-smoke` verified 80 streamed features and nonzero movement/enemy-distance/route-distance deltas plus route-failure ratios from true spawn. Bad milestone: the tiny 8-record smoke still had `shootable_target_steps=0`, `damage_delta=0`, and `max_kills=0`, so the next check needs a longer spawn trend.
- Longer temporal true-spawn trend `ppo-temporal-spawn-trend` ran six 128-transition updates from the independent combat checkpoint. Good signal: temporal features were nonzero in every buffer, route progress stayed positive, and rewards were consistently positive. Bad signal: `visible_enemy_steps=0`, `shootable_target_steps=0`, `damage_delta=0`, and `max_kills=0` across all six updates. Added explicit first-contact curriculum support: first-visible/first-shootable rewards, optional termination on contact, and summary fields for visible/contact events. Follow-up live run `ppo-first-contact-smoke` reached first visible contact from true spawn at record 243, ended that episode with `done_reason=first_visible_enemy`, and recorded `first_visible_contacts=1` plus `contact_reward=10.0`. Still no shootable target, damage, or kill; next target is first-shootable/contact-to-combat transfer.
- Repeated first-visible curriculum training (`ppo-first-visible-train`) produced visible contact in updates 0, 1, and 3; update 3 reached contact much earlier at record 47. Bad milestone: first-shootable probes from both the single first-contact checkpoint and the best first-visible checkpoint failed to reproduce visible contact over 1024 records, with zero shootable-target steps, damage, or kills. Found a learner-control issue: visible-but-not-shootable states were effectively forced into `engage`, so PPO could not choose an alternate contact-closing primitive. Loosened the action mask so visible-but-not-shootable states can choose `seek_enemy` as well as `engage`. Live forced-action checks from the fastest visible reset showed both `engage` and `seek_enemy` still fail to reach shootable contact in 80 steps; a widened-mask first-shootable probe also had zero visible/shootable contact. Next likely unlock is a dedicated contact-to-shootable primitive/curriculum, not just more route reward.
- Added contact-to-shootable control surface. During visible-but-not-shootable contact, PPO can now select `route_progression` toward the protobuf waypoint and `open_use_line` toward farther manual lines around the contact area; `seek_enemy` now targets the visible enemy before falling back to non-visible known enemies. Added named curriculum `e1m1-contact-to-combat` with three validated fresh-reset visible-contact starts plus the known shootable combat start. Bad live evidence remains: forced `engage`, `seek_enemy`, `route_progression`, and `open_use_line` from the fastest visible reset still did not reach shootable contact within 100 macro-steps. Next check is PPO over this contact curriculum.
- Added explicit visible-contact distance-progress reward and rollout summaries (`visible_contact_distance_delta`, `visible_contact_progress_reward`) to bridge protobuf line-of-sight contact into learnable contact-closing signal. Live probe `ppo-contact-progress-probe` over `e1m1-contact-to-combat` confirmed the reward plumbing works (`visible_contact_distance_delta=64.714` then `6.6995`), but still produced `first_shootable_contacts=0`, `damage_delta=0`, and `max_kills=0`. Bad milestone: contact-to-shootable now has scalar feedback, but the remaining gap is likely topology/progressed-state context or a better contact primitive, not just another reward flag.
- Added a stronger visible-contact primitive for PPO: graded direction probes can now use short-clearance rays, visible-contact movement executes as 1-tic adjustments, and `engage`/`seek_enemy` continue the remembered contact corridor after line of sight drops. Forced live probes from `visible_contact_fast` improved from 1 visible step and ~7-9 distance delta to 27 visible steps and `visible_contact_distance_delta=42.2992`, but still did not reach first-shootable contact. Tightened the visible-contact action mask so PPO no longer treats the route waypoint as a sampled contact action; two-update PPO probe improved contact deltas to ~100 units and zero invalid actions, and four-update probe preserved combat-start competence (`damage_delta=95`, `max_kills=1` on `combat_start`) but still had `first_shootable_contacts=0` on the visible-contact stages. Bad milestone: the contact primitive is now learnable and more stable, but the bridge to shootable combat still needs topology/progressed-state context or another local doorway/contact tactic.
- Added short-lived contact use-line memory for PPO. `open_use_line` now remembers a manual line selected during visible contact, can continue approaching it after line of sight drops, and recent-contact masks suppress generic route progression while the contact corridor/use-line remains active. Good live milestone: `ppo-contact-recent-mask-probe` reached the first verified first-shootable contact from the `visible_contact_fast` curriculum stage (`first_shootable_contacts=1`, `shootable_target_steps=1`, `visible_contact_distance_delta=257.7431`, zero invalid actions). Bad follow-up: resuming without `terminate_on_first_shootable` did not reproduce shootable contact or produce damage, so contact-to-damage remains open.
- Tightened the implementation contract after review: added `docs/agent-runtime-contracts.md` to make the decision/controller handshake, concrete memory query/update lifecycle, skill representation, and protobuf-to-observation gap register explicit. This is architecture hardening, not new learning evidence.
- Validated that the first-shootable row from `ppo-contact-recent-mask-probe` is not a valid fresh-reset curriculum start: all row98/row99 coordinate variants reset correctly but had zero line-of-sight and zero shootable target. Added shootable follow-through masking instead. Good live milestone: no-terminate run `ppo-shootable-followthrough-probe` on `visible_contact_fast` reached `first_shootable_contacts=1`, `shootable_target_steps=81`, `fire_on_shootable=76`, `damage_delta=20`, and `max_kills=2`. Bad milestone: `visible_contact_route` and `visible_contact_seek` still produced no shootable contact, damage, or kills, so contact-to-damage is stage-proven but not curriculum-reliable.
- Added fixed curriculum mode so one named reset stage can be repeated with `--curriculum-mode fixed --curriculum-start-index <n>`. Live probe `ppo-fixed-visible-contact-probe` confirmed both updates stayed on `visible_contact_fast`; update 0 reproduced first-shootable contact and damage (`first_shootable_contacts=1`, `shootable_target_steps=15`, `damage_delta=10`), but update 1 regressed to no shootable target or damage. This gives us a better stage-training tool, but the visible-contact boundary still needs repeatability work.
- Added explicit PPO follow-through metrics (`fire_on_shootable_steps`, `missed_shootable_fire_steps`) and a non-promotional `checkpoint_selection_score` so training runs can preserve the best resume checkpoint instead of blindly continuing from the latest update. Live smoke `ppo-best-checkpoint-smoke` verified the output and memory `ppo_best_checkpoint` path, but the short 64-step contact run reached no shootable target or damage, so this is instrumentation/resume hardening rather than new learning progress.
- Added `--resume-best-checkpoint` so PPO training can resume directly from `ppo_best_checkpoint` in memory instead of requiring a hand-copied path. Live smoke `ppo-resume-best-smoke` verified `resume_checkpoint_source=memory_best` and loaded the expected checkpoint, but the 64-step contact run still reached no shootable target or damage. This closes a resume-loop gap; it does not solve contact-stage reliability.
- Tightened the training interface spec after review feedback. `docs/agent-training-interface.md` now spells out the macro-step handshake between PPO and the fast controller, the concrete AgentMemory JSON/query/update contract, what a skill option is today, and why protobuf `GameState` still needs topology/snapshot/recurrent upgrades before full-level PPO can be credible. Exported schemas now carry the same invariants so rollout buffers and checkpoints remain auditable.
- Added contact-route backoff and the first cheap topology observation. After visible contact drops, a failed route-progression macro-step now suppresses generic route sampling while contact recovery is still active; shootable follow-through remains protected by the fire-only mask. PPO observations expanded from 80 to 81 features with `topology_frontier_count_norm`, computed Python-side from open direction probes and low-visit memory cells. This is a learning-surface improvement; live PPO still needs to prove repeatable contact-to-damage gains.
- Live fixed-stage probe `ppo-contact-recovery-long-backoff-fast` resumed the independent combat checkpoint on `visible_contact_fast` for four 512-record updates. Good milestone: 2/4 updates reached first-shootable contact and damage with zero invalid actions; update 0 reached `shootable_target_steps=73`, `damage_delta=30`, `max_kills=2`, and update 3 reached `shootable_target_steps=28`, `damage_delta=33`, `max_kills=2`. Bad milestone: the other two updates still had zero shootable target, damage, or kills, so the stage is improved but not reliable.
- Live round-robin probe `ppo-contact-recovery-round-robin` resumed from the best fixed-stage checkpoint across `visible_contact_fast`, `visible_contact_route`, `visible_contact_seek`, and `combat_start`. Good milestone: combat-start competence was preserved (`damage_delta=140/150`, `max_kills=3`, `shootable_target_steps=279/313` on the two combat updates), and the second `visible_contact_fast` pass reproduced first-shootable contact with `damage_delta=25`, `max_kills=2`. Bad milestone: both `visible_contact_route` and `visible_contact_seek` still produced zero shootable-target steps, damage, or kills, so the contact curriculum is not generalized yet.
- Added option-style contact use-line follow-through. PPO still chooses when to enter `open_use_line`, but once it selects a recent visible-contact manual line, the mask keeps that option active until the line expires or a shootable target appears. Good live milestone: `ppo-contact-line-followthrough-route-probe` converted the previously failing `visible_contact_route` stage into first-shootable contact and damage in 2/3 updates; best update reached `shootable_target_steps=54`, `damage_delta=25`, `max_kills=2`, and zero invalid actions. Transfer to `visible_contact_seek` using the route-stage checkpoint also produced first-shootable contact, `shootable_target_steps=62`, `damage_delta=30`, and `max_kills=2` in its best update. Bad milestone: both probes still had one zero-shootable update, so contact-to-combat is improved but not repeatable enough to promote to full-level PPO.
- Follow-up round-robin probe `ppo-contact-followthrough-round-robin` resumed from the best seek-stage checkpoint across `visible_contact_fast`, `visible_contact_route`, `visible_contact_seek`, and `combat_start`. Good milestone: `visible_contact_fast` reached `shootable_target_steps=75`, `damage_delta=20`, `max_kills=2`; `visible_contact_route` reached `shootable_target_steps=40`, `damage_delta=30`, `max_kills=2`; and `combat_start` preserved combat with `shootable_target_steps=97`, `damage_delta=50`, `max_kills=1`. Bad milestone: the `visible_contact_seek` pass still regressed to zero shootable target, damage, or kills, so the remaining contact generalization gap is now narrower but still real.
- Tightened contact use-line execution after a bad bypass probe. Bad milestone: `ppo-contact-line-bypass-seek-probe` let contact-selected use lines ignore ordinary blacklisting, but overcommitted to `open_use_line`; all three 256-record updates had `shootable_target_steps=0`, only the last reached `damage_delta=15`, and `max_kills=0`. Added a bounded contact-line primitive that directly approaches/presses remembered contact lines and releases forced follow-through after a long same-skill streak unless the line is close enough to use. Mixed follow-up milestone: `ppo-contact-line-guarded-seek-probe` still over-sampled `open_use_line`, but recovered some action diversity and reached `damage_delta=30` in two updates with `max_kills=1` in the last update. This is not a promotion-worthy fix; it is evidence that the contact boundary needs explicit local topology/progressed-state context, not just longer option forcing.
- Added `restfuldoom.learning_trace.v1` to PPO rollouts. Each collected transition now carries a compact named trace that ties the pre-action observation groups, action mask, selected skill, controller decision, reward, route/contact outcome, and transition deltas together. This does not change training behavior, but it directly addresses the audit/debugging gap between "we have protobuf state" and "we can explain what the learner saw and why it chose a skill."
- Added explicit contact-context PPO observations. The vector expanded from 81 to 89 features by appending recent-contact active state plus current/remembered contact use-line active flag, distance, angle, close-use readiness, bounded follow-through flag, and age. Older checkpoints can resume through the existing prefix-preserving observation migration. This is observation-surface progress only; the next live milestone must show that PPO uses these features to make `visible_contact_seek` or full contact curriculum more repeatable.
- Added contact-context rollout summary metrics so PPO buffers report how often recent contact, contact use-lines, close-use readiness, and bounded follow-through were active. Live fixed-stage probe `ppo-contact-context-seek-probe` resumed the previous 81-feature seek checkpoint into the new 89-feature model and trained `visible_contact_seek` for four 512-record updates. Good milestone: all four updates reached `max_kills>=1`, three reached shootable/fire behavior, and the best two reached `shootable_target_steps=117/150`, `fire_on_shootable=101/130`, `damage_delta=56/35`, and `max_kills=2`. Bad milestone: the learner still overuses contact-line forcing, and damage can occur without ordinary shootable/fire windows, so this is not a full promotion signal.
- Round-robin transfer probe `ppo-contact-context-round-robin` from the new seek checkpoint was mixed. Good milestone: `visible_contact_route` reached `shootable_target_steps=47`, `damage_delta=20`, `max_kills=2`, and `visible_contact_seek` reached `shootable_target_steps=44`, `damage_delta=25`, `max_kills=2`. Bad milestone: `visible_contact_fast` regressed to zero shootable target/damage/kills, and `combat_start` damaged enemies (`damage_delta=34`) but scored no kills. Current conclusion: explicit contact context helps the previously failing seek/route stages, but fixed-stage training can still over-specialize and hurt generalization/combat-start competence.
- Added optional checkpoint curriculum evaluation for PPO. `--checkpoint-eval-curriculum` now evaluates each saved checkpoint across every active curriculum stage, stores `restfuldoom.ppo_checkpoint_curriculum_eval.v1`, and lets `ppo_best_checkpoint` prefer cross-stage competence over a single rollout's local reward. Because eval and rollout scores are not comparable, eval-scored checkpoints supersede older rollout-only bests and then compare against later eval-scored checkpoints. This is not new learning evidence yet; it is a guardrail added because the latest contact-context run showed fixed-stage overfitting and combat-start regression.
- Live smoke `ppo-checkpoint-curriculum-eval-smoke` ran one tiny update with checkpoint curriculum eval against all four `e1m1-contact-to-combat` stages. Good milestone: training output and memory `ppo_best_checkpoint` carried `restfuldoom.ppo_checkpoint_curriculum_eval.v1`, `checkpoint_selection_source=checkpoint_curriculum_eval`, and per-stage scores. Bad/expected milestone: it was intentionally too short to learn (`max_kills=0`, `damage_delta=0`), so this proves the selection/eval plumbing rather than policy improvement.
- Added local topology-context PPO observations. The vector expands from 89 to 96 features by appending current-cell visit pressure, minimum/mean visits among open projected neighbor cells, active frontier flag, frontier angle, and exhausted-open-cell ratio. Rollout summaries now expose topology frontier/exhaustion metrics. This preserves old checkpoint prefixes through append-only observation migration. It is observation-surface progress for spawn-to-contact, not evidence that the agent learned the full level yet.
- Live smoke `ppo-topology-context-smoke` resumed the 89-feature contact-context checkpoint into the 96-feature model and saved a checkpoint with `restfuldoom.ppo_observation_migration.v1` (`from_obs_dim=89`, `to_obs_dim=96`, optimizer state not loaded). The 16-record rollout emitted topology metrics (`mean_topology_current_cell_visits_norm=1.0`, `mean_topology_exhausted_open_ratio=1.0`) and kept all records in visible contact, but reached no damage or kills. This validates the runtime observation/migration path, not learning quality.
- Added within-rollout curriculum stage mixing for PPO. `--rollout-stage-mix round_robin|random` can switch reset starts between `done=True` episodes inside one PPO buffer, with optional `--rollout-stage-segment-tics` to force short reset episodes during probes. Rollout JSONL now preserves per-record curriculum metadata instead of flattening mixed buffers to one stage. This is a regularization/debugging tool for fixed-stage overfitting; it is not a substitute for the real next unlock, which is snapshot-backed progressed-map curriculum through Hellbox/Shrink.
- Live smoke `ppo-mixed-stage-smoke` ran one 24-record mixed rollout against the Docker gRPC server with `--rollout-stage-mix round_robin --rollout-stage-segment-tics 8`. The rollout summary and JSONL rows preserved all four stages in one PPO buffer (`combat_start=8`, `visible_contact_fast=3`, `visible_contact_route=8`, `visible_contact_seek=5`) with zero invalid actions. Bad/expected milestone: this tiny smoke reached no damage or kills, so it validates live mixed-stage collection rather than policy improvement.
- Added the first snapshot-backed curriculum contract for progressed-map PPO. `restfuldoom.snapshot_curriculum.v1` manifests can now feed `ppo_agent --snapshot-curriculum`; snapshot stages run an external restore command, use `reset_mode=snapshot`, reconnect to the restored gRPC stream, and explicitly skip `ResetEpisode` so opened doors/enemy movement are not erased. Rollout rows carry `restfuldoom.reset_context.v1`, summaries expose snapshot restore counts/stage counts/timing, and training-job exports include snapshot curriculum metadata plus local snapshot artifacts. This is the missing training primitive shape, but not yet evidence of full independent PPO level completion until real Hellbox/Shrink snapshots are captured and trained through the promotion gate.
- Live hook smoke `ppo-snapshot-hook-smoke` used a fake local snapshot manifest and `--snapshot-restore-command true` against the Docker gRPC server. The 8-record rollout wrote `reset_context.source=snapshot_restore`, `skipped_reset_episode=true`, `snapshot_restore_count=1`, and `snapshot_stage_counts={"live_stream_snapshot_hook": 8}` with zero invalid actions. This validates the Python snapshot-reset path and metadata on a live stream; it still does not prove actual Hellbox/Shrink restore until real VM snapshots replace the no-op restore command.
- Added snapshot curriculum builder/validator workflow. `restfuldoom_agent.snapshot_builder` can now turn trajectory rows or auto milestones (`first-visible`, `first-shootable`, `first-damage`, `first-kill`, `level-transition`) into `restfuldoom.snapshot_curriculum.v1` capture plans; `restfuldoom_agent.snapshot_curriculum --validate --require-artifacts` checks local snapshot files and `sha256:` digests. The Hellbox demo script now exposes `snapshot-plan` and `snapshot-validate`. This closes manual JSON surgery around snapshot curriculum, but it is not new learning evidence until real VM snapshot artifacts are captured and PPO trains/evals from them.
- Added native agent save-slot snapshot plumbing. The proto now exposes `SaveSnapshot`/`LoadSnapshot`; Rust queues fixed-size save/load control requests; the C bridge applies them on the Doom simulation thread; agent saves use separate `agentdoomN.dsg` files; and `DoomAgentEnv.reset(reset_mode="snapshot")` can restore via gRPC when a manifest stage has `snapshot.slot` or `snapshot.ref=save_slot:N`. The current ticcmd is suppressed after reset/load to avoid applying a stale command to the restored state. This is a major progressed-state reset primitive for PPO, but it is not yet full-level PPO learning evidence until we capture real bottleneck slots and train/evaluate through the promotion gate.
- Added native snapshot capture and hard restored-state verification. `restfuldoom_agent.snapshot_capture` now runs the structured brain, captures native save slots at live protobuf milestones (`first-visible`, `first-shootable`, `first-damage`, etc.), and writes a PPO-ready `restfuldoom.snapshot_curriculum.v1` manifest with `save_slot:N` refs. Snapshot resets now compare the first restored protobuf state against stage `expected_state` by default and fail on drift; PPO summaries report verification counts/failures. This closes the "queued load is not proof" gap, but the next learning milestone still has to train PPO from captured slots and beat fresh-reset curriculum on contact/damage/kills.
- Hardened native save-slot verification against live Docker. Rebuilding the container fixed a stale `UNIMPLEMENTED` `SaveSnapshot` server, and the first capture run produced four slots (`first-visible`, `first-shootable`, `first-damage`, `first-kill`) but exposed a bad verifier assumption: `GameState.tick` is a live stream counter, while Doom save/load preserves `level_time`. The verifier now checks saved `level_time` by default and only checks stream tick when explicitly requested. A follow-up live capture `e1m1-native-progressed-bottlenecks-v2` captured two slots and hard-verified the first-visible slot; the first-shootable slot then verified after aligning `shootable_target` with protobuf `combat.has_shootable_target` and keeping `target_is_enemy` as its own optional field. Short PPO smoke `ppo-native-slot-v2-smoke-verify` restored the two native slots 6 times with `snapshot_verification_failures=0` over 48 real transitions. Bad milestone: the v2 capture got stuck around cell `9:-23`, reached no damage or kills over 16,000 records, and confirms repeatability/seed application is still a blocker before this becomes a reliable full-level PPO curriculum.
- Added multi-attempt native snapshot capture. `snapshot_capture --attempts N --reset-before-attempt` now runs fresh capture attempts with a new structured-brain policy instance and optional fast gRPC `ResetEpisode` before each stream, writing per-attempt trajectories and recording attempt metadata in the manifest. A bad live run also exposed a slot-capacity bug: native agent saves are clamped to slots `0..9`, so requesting slots `10..13` silently overwrote slot 9 at the server. The capture CLI now rejects exhausted ranges and fails if the server normalizes a requested slot. Valid live capture with slots `3..6` produced and hard-verified four progressed milestones (`first-shootable`, `first-visible`, `first-damage`, `first-kill`) in 1,483 records. PPO smoke `ppo-native-multi-slot-smoke` consumed that manifest for 128 transitions with 10 snapshot restores, `snapshot_verification_failures=0`, `shootable_target_steps=20`, `fire_on_shootable_steps=20`, and `max_kills=1`. This is strong reset/curriculum evidence, but still not a full promotion run or proof of spawn-to-exit PPO competence.
- Added earned kill accounting for snapshot-backed PPO summaries. Absolute `max_kills` can be inherited from a restored `first-kill` slot, so summaries now also report `kill_delta`, `max_kill_gain`, `snapshot_kill_delta`, and `snapshot_max_kill_gain`; checkpoint selection scores use earned kills rather than inherited map counters. Live smoke `ppo-earned-kill-smoke` consumed the valid slots for 64 transitions with 5 snapshot restores, 0 verification failures, `shootable_target_steps=10`, `fire_on_shootable_steps=10`, `kill_delta=1`, `max_kill_gain=1`, and `snapshot_kill_delta=1`. Good milestone: the short run earned a kill after restore and the score no longer needs to trust absolute kill counters. Bad/limited milestone: this is still a snapshot curriculum smoke, not full spawn-to-exit PPO competence.
- Fixed the same inherited-counter leak in checkpoint curriculum eval. Eval now runs each curriculum stage through the same reset preparation path as PPO collection, so native slot stages actually call gRPC `LoadSnapshot` instead of degrading to fresh teleport starts. `EpisodeEval` records `reset_source`, start/end map, `start_kills`, earned `kill_delta`/`max_kill_gain`, and item/secret deltas/gains. Live smoke `ppo-snapshot-eval-fields-smoke` verified the first-kill snapshot eval reported `start_kills=1`, `max_kills=1`, `kill_delta=0`, `max_kill_gain=0`, `mean_kills=0.0`, and `reset_source=snapshot_restore`; the rollout side restored 4 snapshots with 0 verification failures and earned `kill_delta=1`. This makes best-checkpoint selection harder to fool, but it is still an eval-hygiene milestone rather than full-level PPO competence.
- Added `first-enemy-shootable` snapshot selection after the previous `first-shootable` slot proved too broad (`target_is_enemy=false` can capture non-enemy targets). Live native capture `e1m1-enemy-shootable-bottlenecks` saved slots 0..2 for `first-visible`, `first-enemy-shootable`, and `first-damage`; all three hard-verified, and the enemy-shootable slot restored with `shootable_target=true` and `target_is_enemy=true` at level_time ~1419. PPO continuation `ppo-enemy-slot-train` resumed the prior contact checkpoint for three 256-record updates on this cleaner curriculum: every update had 0 snapshot verification failures, `kill_delta=5`, `damage_delta=95/95/100`, and checkpoint eval earned kills on both `first-enemy-shootable` and `first-damage` stages (`mean_kills=1.0` for each). Good milestone: corrected curriculum data improved eval selection score from the old native-slot run's ~46.98 to ~78.44. Bad milestone: `first-visible` still earned no kill or useful reward, so visible-contact-to-shootable remains the next PPO bottleneck.
- Added explicit visible-contact geometry to the PPO observation contract. The vector expanded from 96 to 104 features by appending active/shootable/needs-closure flags, nearest visible enemy distance, signed angle, alignment, and close-contact state. Rollout traces now include a `visible_contact` group and summaries expose active/closure/shootable/aligned/close counts plus mean distance. Live smoke `ppo-visible-contact-context-smoke` against the Docker gRPC server verified the new JSONL group (`visible_contact_active=1`, `visible_contact_needs_closure=1`, angle/distance populated) and summary metrics (`visible_contact_active_steps=4`, `visible_contact_needs_closure_steps=4`, `mean_visible_contact_distance_norm=0.9702`). Bad milestone: the 16-record run still over-selected `open_use_line`, reached `first_shootable_contacts=0`, and produced no damage/kills, so this is observation-surface validation, not proof of improved learning.
- Live migrated snapshot probe `ppo-visible-context-snapshot-probe` resumed the 96-feature enemy-slot checkpoint into the 104-feature model (`restfuldoom.ppo_observation_migration.v1`, `from_obs_dim=96`, `to_obs_dim=104`, optimizer state not loaded) and trained two 128-record updates over the native `first-visible`/`first-enemy-shootable`/`first-damage` curriculum. Good milestone: snapshot restore verification stayed at 0 failures, all 256 buffer records carried the new `visible_contact` trace group, update 1 reached `visible_contact_active_steps=112`, `visible_contact_needs_closure_steps=42`, `visible_contact_shootable_steps=70`, `shootable_target_steps=72`, `fire_on_shootable_steps=70`, `damage_delta=55`, and `kill_delta=3`. Bad milestone: checkpoint curriculum eval still scored the `first-visible` stage at 0 earned kills, so the exact first-visible-to-shootable gap remains open even though the new observation is live and compatible with snapshot training.
- Added a first-class PPO `close_visible_contact` option after diagnosing that far `first-visible` starts could overcommit to `open_use_line` before the enemy was close or aligned. The action schema now has 9 skills, legacy 8-action observations pad with a neutral `prev_skill_close_visible_contact` bit, and old checkpoints can migrate by expanding both observation columns and actor action rows. The mask now exposes `close_visible_contact` for visible-but-not-shootable states and gates far `open_use_line` until the line is close/use-ready or the enemy contact is close and aligned. Bad/unknown milestone: this is option-surface progress only until a live first-visible snapshot eval shows better first-shootable/damage/kill rates.
- Live validation of the new contact option passed the plumbing gate but not the learning gate. Loading the old 89-feature/8-action `ppo-contact-context-seek-probe-ppo-0003.pt` checkpoint into the 105-feature/9-action schema produced `restfuldoom.ppo_checkpoint_migration.v1` with observation and action expansion. A 32-step fixed `visible_contact_fast` PPO smoke against Docker selected only `close_visible_contact` and `seek_enemy` (`skill_counts={"close_visible_contact":15,"seek_enemy":17}`), had `invalid_action_steps=0`, `visible_contact_needs_closure_steps=30`, and no `open_use_line` overcommit. Bad milestone: it still reached `first_shootable_contacts=0`, `damage_delta=0`, and `kill_delta=0`; also, the checked-in native slot manifest is stale in the current container (`level_time` drift 264 on slot 4), so snapshot smoke must recapture slots before eval claims.
- Added `restfuldoom_agent.forced_option_eval` for forced primitive micro-evals from ordinary or snapshot curriculum stages. It compares selected skills from the same restored state, writes JSONL records, and reports forced-mask validity, decision-skill counts, stuck/recovery steps, visible-contact loss, contact-line close steps, enemy-distance delta, first-shootable contacts, damage, and earned kills. Recaptured current Docker native slots 0..2 for `first-visible`, `first-enemy-shootable`, and `first-damage`; all hard-verified. Good live milestone: after adding contact-line approach/follow-through plus bounded contact-line stall release to `close_visible_contact`, the first-visible forced eval beat `seek_enemy` on distance closure (`enemy_distance_delta=1218.3611` vs `25.8768`), reached `first_shootable_contacts=1`, `shootable_target_steps=3`, `visible_contact_distance_delta=468.4015`, and did `damage_delta=3`. Bad milestone: it still had `kill_delta=0`, `fire_on_shootable_steps=0`, and 2 invalid/mask-disallowed close-option steps, so this is the first visible-to-shootable bridge signal, not a promotion-worthy combat policy yet.
- Added a forced-eval shootable handoff and PPO skill allowlist for exact contact-bridge ladder tests. The forced evaluator can now switch from `close_visible_contact` to `fire` once the normal action mask makes fire available; selected actions still respect the mask, and reports split original forced-disallowed steps from true selected invalid steps. Recaptured current Docker native slots `0..2` into `trajectories/ppo/e1m1-native-first-visible-current.json` and verified snapshot restores. Good live milestone: forced `close_visible_contact` from the current first-visible slot now reaches `first_shootable_contacts=1` with zero invalid actions, and `close_visible_contact` with `--shootable-handoff-skill fire` reached `shootable_target_steps=32`, `fire_on_shootable_steps=29`, `damage_delta=20`, `kill_delta=2`, `snapshot_verification_failures=0`, and `selected_disallowed_steps=0`. Added `ppo_agent --allowed-skill` and `--eval-checkpoint --checkpoint-eval-curriculum` for exact ladder evals without extra training. Mixed unconstrained PPO preserved shootable/damage-stage kills but still earned zero first-visible kills. Constrained PPO improved the bottleneck: `ppo-close-seek-fire-first-visible-current` update 0 reached `first_shootable_contacts=1`, `shootable_target_steps=44`, `fire_on_shootable_steps=42`, `damage_delta=20`, and `kill_delta=1` from the first-visible slot, and 3-episode eval of checkpoint `ppo-close-seek-fire-first-visible-current-ppo-0001.pt` earned first-visible kills in 2/3 episodes (`kill_delta=[2,0,2]`, mean kills `1.3333`) while preserving stronger shootable stages (`first-enemy-shootable` mean kills `4.0`, `first-damage` mean kills `3.0`). Bad milestone: first-visible still has a zero-kill eval episode and rollout update 1 regressed to no shootable/damage, so the bridge is materially improved but not reliable enough for full promotion or spawn-to-level claims.
- Fixed a PPO eval hygiene issue that made multi-episode snapshot eval less trustworthy: `SkillController.reset_episode_context()` cleared PPO-facing action history, but the wrapped deterministic `BrainPolicy` still carried episode-local tactical state such as line-attempt blacklists, blocked targets, contact rays, stuck timers, hazard escape state, and per-episode cell visits. Added `BrainPolicy.reset_episode_context()` and call it from the PPO controller while preserving persistent `AgentMemory`. Added per-episode checkpoint-eval diagnostics for skill counts, visible/shootable contacts, fire-on-shootable, damage, invalid actions, and snapshot verification failures. Good live milestone: rerunning the constrained checkpoint eval after the reset fix made the previous first-visible zero-kill episode disappear. `eval-close-seek-fire-first-visible-reset-clean` earned kills in all three first-visible episodes (`kill_delta=[2,2,2]`, `damage_delta=[25,35,35]`, `first_shootable_contacts=1` each, `invalid_action_steps=0`, `snapshot_verification_failures=0`), preserved `first-enemy-shootable` at `kill_delta=[4,4,4]`, and preserved `first-damage` at `kill_delta=[3,3,3]`. Bad/limited milestone: this is a corrected constrained snapshot-eval result, not proof of full spawn-to-exit PPO competence or broad intelligence.
- Ran short constrained first-visible PPO continuation after the reset hygiene fix. `ppo-first-visible-reset-clean-train` resumed `ppo-close-seek-fire-first-visible-current-ppo-0001.pt` with allowed skills `close_visible_contact`, `seek_enemy`, and `fire`, fixed to the first-visible native snapshot, and checkpoint-evaluated every save across first-visible, first-enemy-shootable, and first-damage. Good milestone: checkpoint `0000` evaluated cleanly across all stages: first-visible `kill_delta=[2,2,2]`, `damage_delta=[30,20,30]`, `first_shootable_contacts=1` for each episode, plus shootable/damage stages at `kill_delta=[4,4,4]` and `[3,3,3]`, all with `invalid_action_steps=0` and `snapshot_verification_failures=0`. Bad milestone: the next PPO update's rollout reached first-shootable and damage (`shootable_target_steps=29`, `damage_delta=20`) but its checkpoint eval regressed first-visible to `kill_delta=[2,0,2]`; the cross-stage eval correctly kept `0000` as best. This confirms the bridge is real but still sensitive to PPO updates, so promotion should stay eval-gated.
- Ran the mixed snapshot-curriculum ladder step. `ppo-mixed-reset-clean-bridge` resumed the stable constrained checkpoint and mixed first-visible, first-enemy-shootable, and first-damage native snapshots in one PPO buffer with `rollout-stage-mix=round_robin`, allowed skills `close_visible_contact`, `seek_enemy`, and `fire`, and checkpoint-evaluated the saved policy across all three stages. Good milestone: the 384-record mixed rollout restored 6 snapshots with 0 verification failures, had 0 invalid actions, covered all three stages (`first-visible=103`, `first-enemy-shootable=204`, `first-damage=77` records), reached `shootable_target_steps=180`, `fire_on_shootable_steps=177`, `damage_delta=75`, and earned `kill_delta=6`. Checkpoint eval stayed reliable: first-visible `kill_delta=[2,2,2]` with `first_shootable_contacts=1` each, first-enemy-shootable `kill_delta=[4,4,4]`, and first-damage `kill_delta=[3,3,3]`, all with 0 invalid actions and 0 snapshot verification failures. Bad/limited milestone: the mixed checkpoint's aggregate score (`351.0586`) did not beat the older global best constrained score, and this still proves bottleneck curriculum competence rather than full spawn-to-exit PPO autonomy.
- Hardened best-checkpoint resume selection against stale eval protocols. The previous memory best was a one-episode-per-stage checkpoint eval, while the reset-clean and mixed bridge gates use three episodes per stage plus richer diagnostics. Best-checkpoint replacement now ranks checkpoint-curriculum eval protocols by deterministic-vs-sampled mode, stage count, episodes per stage, and max steps; a stricter protocol can supersede weaker evidence only if it retains at least 85% of the previous score, preventing obviously weaker checkpoints from winning solely because they ran more episodes. Re-recorded the mixed checkpoint through that rule, so `ppo_best_checkpoint` now points at `ppo-mixed-reset-clean-bridge-ppo-0000.pt` with three-episode cross-stage eval evidence. This improves resume hygiene; it is not new gameplay competence by itself.
- Ran a true-spawn transfer eval from the mixed best checkpoint with the full skill set restored. Bad milestone: `eval-mixed-best-true-spawn-transfer` showed no transfer to spawn-to-contact. Candidate and heuristic baseline both selected `seek_enemy` for all 1,605 macro steps, saw `visible_enemy_steps=0`, `first_shootable_contacts=0`, `damage_delta=0`, and `kill_delta=0`; promotion correctly failed on completion and kills. Current conclusion: the first-visible snapshot bridge is stable, but true-spawn PPO still needs a spawn-to-first-contact gate that prevents blind known-enemy chasing from dominating route/exploration behavior before any visible contact has been established.
- Added a blind-seek gate for true-spawn transfer: persistent/global enemy memory no longer exposes `seek_enemy` before the current episode has visible-contact evidence, so route/exploration stays available instead of immediately chasing stale enemy coordinates through walls. Good live milestone: rerunning true-spawn eval as `eval-mixed-best-true-spawn-gated` moved the candidate from zero contact to `first_visible_contacts=1` and `visible_enemy_steps=19` with `route_progression=152` and 0 invalid actions. Bad milestone: it still failed to convert contact into shootable combat (`first_shootable_contacts=0`, `damage_delta=0`, `kill_delta=0`) and then over-selected `seek_enemy=1372` after contact; the heuristic baseline, while mask-invalid in places, reached `first_shootable_contacts=1`, `damage_delta=85`, and `kill_delta=4`. Next bottleneck is post-contact action discipline from spawn: recent visible contact should stay in `close_visible_contact`/contact recovery long enough to become shootable, rather than falling back to broad known-enemy seeking.
- Tightened post-contact action discipline and added a low-health survival override. Broad `seek_enemy` is now suppressed for the entire active recent-contact window, so `close_visible_contact` owns lost-contact recovery until the contact expires; when health is at or below `retreat_health` with visible/recent contact, the PPO mask forces the deterministic `retreat` option. Good live milestone: strict contact gating alone produced the first true-spawn PPO combat transfer: `eval-mixed-best-true-spawn-contact-strict` reached `first_shootable_contacts=1`, `shootable_target_steps=229`, `damage_delta=105`, and `kill_delta=5` with 0 invalid actions. Bad milestone: it died (`min_health=-9`, `survival_rate=0`). With the survival gate, `eval-mixed-best-true-spawn-survival-gated` stayed alive and improved to `damage_delta=164`, `kill_delta=6`, `item_delta=3`, `min_health=90`, `survival_rate=1.0`, and 0 invalid actions, beating the heuristic baseline on kills/reward while still failing promotion only because level completion remained 0. A longer 12k eval preserved alive 6-kill/3-item behavior but timed out in route/recover loops (`route_progression=2367`, `recover_stuck=426`, `level_completed=false`). Current bottleneck has moved from contact/combat/survival to post-combat exit routing and stuck recovery.
- Added candidate eval tracing (`restfuldoom.ppo_eval_trace.v1`) plus collect-only deterministic rollout support so promotion-eval behavior can be inspected step by step without running PPO updates. Live traces were mixed and exposed repeatability risk rather than a new promotion state: one traced current-code eval ended in a low-health retreat-only timeout at 4 kills/1 item, a too-short retreat cooldown restored action but died at 4 kills, and stale broad `seek_enemy` could still collapse to 0 kills after first contact expired. Gated blind seek now requires recent visible evidence instead of any historical `_last_visible_enemy_id`, and low-health no-LOS contact uses a bounded retreat streak before reopening recovery. A follow-up live trace `eval-mixed-best-true-spawn-retreat96-stale-seek` reached 5 kills/1 item and survived to max steps with 23 health, but still did not complete the level. Added two controller hardening fixes recommended by a read-only Claude review: exit-switch press/backtrack decisions no longer count as locomotion for stuck detection, and progression-line attempts are no longer blacklisted for 1200 tics. These are diagnostic/control-surface improvements; full true-spawn PPO level completion remains unproven.
- Hardened the first-visible ladder tooling. Forced option eval now terminates cleanly if the forced skill becomes mask-disallowed before a shootable handoff, instead of stepping a known-disallowed action. PPO allowlists now have opt-in strict mode (`--strict-allowed-skills`) plus rollout/eval counters for allowed-skill fallback pressure, so an "exact" bottleneck ladder cannot silently escape into route/recover skills. Good live milestone: current Docker Gate A forced `close_visible_contact` from `0500-first-visible_snapshot` reached `first_shootable_contacts=1` in 53 records with 0 invalid/selected-disallowed steps and `termination_reason=first_shootable_target`; Gate B with fire handoff reached `shootable_target_steps=36`, `fire_on_shootable_steps=25`, `damage_delta=15`, `kill_delta=1`, 0 selected-disallowed steps, and 0 snapshot verification failures. Strict checkpoint ladder `eval-mixed-reset-clean-current-strict-ladder-fields-full` evaluated `ppo-mixed-reset-clean-bridge-ppo-0000.pt` across first-visible, first-enemy-shootable, and first-damage native snapshots with allowed skills only `close_visible_contact`, `seek_enemy`, and `fire`; skill counts contained only `close_visible_contact` and `fire`, invalid actions were 0, snapshot failures were 0, and earned kills were first-visible `[2,1,2]`, enemy-shootable `[2,2,2]`, and first-damage `[2,2,2]`. Bad/diagnostic milestone: strict fallback pressure was 0 on first-visible but 50-51 `close_visible_contact` fallback steps per episode on the later shootable/damage stages, meaning strict mode prevented skill leakage but still had to synthesize an in-allowlist fallback after the raw mask wanted non-allowlisted behavior. This improves ladder honesty; it is still bottleneck-curriculum competence, not full true-spawn level completion.
- Added an earned-kill termination gate for focused bottleneck curricula (`--terminate-on-required-kills`). This stops snapshot combat episodes once post-restore earned kills reach `--required-kills`, separating the bridge objective from unrelated post-kill route/recovery behavior. Good live milestone: forced first-visible `close_visible_contact` plus fire handoff with kill termination stopped at `done_reason=required_kills`, `kill_delta=1`, `shootable_target_steps=48`, `fire_on_shootable_steps=47`, `invalid_action_steps=0`, `selected_disallowed_steps=0`, and 0 snapshot verification failures. Kill-terminated strict checkpoint eval of `ppo-mixed-reset-clean-bridge-ppo-0000.pt` earned 1 kill in all 9 episodes across first-visible, first-enemy-shootable, and first-damage snapshots, all with `done_reason=required_kills`, 0 invalid actions, 0 snapshot failures, and 0 strict fallback steps; first-visible reached `first_shootable_contacts=[1,1,1]`, `shootable_target_steps=[48,48,48]`, and `fire_on_shootable_steps=[47,47,47]`. PPO continuation `ppo-first-visible-killterm-train` ran two 256-record updates from the first-visible slot using a temporary memory copy; both rollouts earned `kill_delta=1` with 0 invalid and 0 strict fallback steps, and checkpoint eval kept all three stages at `kill_delta=[1,1,1]`. Bad/limited milestone: update 1 did not beat update 0 (`selection_score` 154.8977 vs 155.9223), and this still proves a restored bottleneck skill loop rather than full spawn-to-exit PPO competence.
- Added required-kill speed scoring for bottleneck checkpoint selection. `EpisodeEval` now records `steps_to_required_kills`, and checkpoint-curriculum stage scores include a bounded speed bonus only when episodes end with `done_reason=required_kills`; this prevents long post-shootable windows from looking better merely because they accumulate more action reward. Live eval smoke `eval-killterm-speed-smoke` verified the JSON contract and formula, with `steps_to_required_kills` 164 for first-visible, 54 for first-enemy-shootable, and 9 for first-damage, all with `kill_delta=1` and 0 strict fallback steps. Live PPO smoke `ppo-first-visible-killterm-speed-score-smoke` used temporary memory and selected its checkpoint from the new speed-aware curriculum score (`231.738`): rollout earned `kill_delta=1`, `shootable_target_steps=48`, `fire_on_shootable_steps=47`, 0 invalid actions, and 0 strict fallback steps; checkpoint eval reached required kills on all three stages with first-visible `steps_to_required_kills=137`, enemy-shootable `53`, and first-damage `9`. Bad/limited milestone: this improves selection pressure inside the restored bottleneck curriculum; it does not yet address true-spawn routing or level completion.
- Fixed a bad incentive in the first version of required-kill speed scoring. A read-only Claude review and live 4-update probe `ppo-first-visible-killterm-speed-run4` showed the initial speed bonus was still dominated by accumulated shaped reward: update 3 was selected despite slower first-visible kills because it collected more reward before termination. The score is now `restfuldoom.ppo_checkpoint_eval_score.v2`: required-kill stages use earned-kill reliability plus speed, with shaped reward capped as a small tiebreaker and per-stage score components emitted in checkpoint eval JSON. Recomputing the 4-update run under v2 would pick update 1 instead of update 3 because update 1 had faster first-visible required-kill completion (`mean_steps_to_required_kills=165.3333` vs `229.0`). Live smoke `eval-killterm-v2-score-smoke` verified `score_schema=restfuldoom.ppo_checkpoint_eval_score.v2`, per-stage `required_kill_speed_bonus`, and capped `reward_tiebreak=20.0`; it earned kills on all three native snapshot stages with 0 strict fallback steps. Bad/limited milestone: this is selection-hygiene progress, not new full-level learning evidence.
- Validated the v2 required-kill scoring with a fresh 4-update live run `ppo-first-visible-v2-speed-run4`. Every checkpoint-eval episode across first-visible, first-enemy-shootable, and first-damage earned the required post-restore kill with 0 invalid actions, 0 strict allowed-skill fallback, and 0 snapshot verification failures. The best checkpoint was update 2 (`selection_score=340.7688`), selected because it improved required-kill speed: first-visible required-kill steps were `[222, 137, 137]`, enemy-shootable `[53, 54, 53]`, and first-damage `[8, 9, 8]`; shaped reward was capped at `20.0` as a tie-breaker in all stages. A parallel read-only subagent review found an eval hygiene gap: `DoomAgentEnv` records current restore checks as `restored_state_verification`, while PPO eval only inspected older verification key names. Fixed that counter path and added a regression test so failed snapshot restores are counted in `EpisodeEval.snapshot_verification_failures`. Bad/limited milestone: bottleneck snapshot kill behavior is reliable under the strict restored-state gate, but true-spawn E1M1 completion remains unproven. Next honest gate is either full true-spawn promotion (`level_completion_rate=1.0`, survival, kills, zero invalid actions, no allowlist crutches) or a narrower post-combat exit-routing snapshot gate that proves level transition from progressed 5+ kill state without overclaiming full-level PPO competence.
- Hardened training-job portability for Docker-to-cloud resume. `restfuldoom.training_job.v1` export now includes referenced PPO rollout buffers in addition to memory, notes, skill models, PPO checkpoints, trajectories, snapshot artifacts, schemas, reward config, and eval history. Import now rewrites extracted memory references for learned checkpoints, PPO latest/best/lineage checkpoints, PPO buffers, episode trajectories, and snapshot artifact paths to destination-local files, and stamps `restfuldoom.training_job_import.v1` metadata with a path rewrite count. Good milestone: a regression test exports a bundle, imports it into a different directory, and verifies the imported memory points at local checkpoint/buffer/trajectory/snapshot files instead of the source tree. A real current-memory smoke exported/imported 1 skill model, 113 PPO checkpoints, 113 PPO rollout buffers, and 50 trajectories, rewrote 325 path references under the imported directory, and verified the selected learned/best PPO paths exist locally. Remaining gap: native Doom `save_slot:N` curricula are now reported as unbundled slots in the manifest (3 in the current smoke), but they still need actual `agentdoomN.dsg` save files plus install/checksum metadata before snapshot curricula are fully portable across machines.
- Added the post-combat exit-routing curriculum gate and diagnostics after the true-spawn bottleneck moved from combat to route/recover loops. Snapshot builder/capture now support `post-combat` and `post-combat-exit-route` selectors with a configurable `--post-combat-kills` threshold and route-waypoint restored-state verification. The `post-combat-exit-route` selector is now strict: it requires `route_waypoint.exit=true` plus a positive route line id, so older exit-control skills/use-lines alone no longer seed an exit-route slot. Checkpoint eval score schema is now `restfuldoom.ppo_checkpoint_eval_score.v3`; level-complete stages use `exit_routing_speed` scoring so faster exits beat slow high-shaping rollouts, and eval aggregates now include `reset_source_breakdown` plus snapshot verification failure counts for promotion vetoes. Rollout summaries split exit-route attempts/reaches/failures/progress from generic route metrics. Good milestone: all 216 Python agent tests passed, and the known successful `brain-train-68-current-success.jsonl` now builds a valid `post-combat-exit-route` snapshot plan from row 2119. Bad milestone: a live current Docker capture attempt for post-combat exit slots never reached the gate (`max_kills=2`, `max_items=2`, 10,945 rows, last skill `cross_progression_line`), so this is gate/readiness work and repeatability evidence, not new PPO learning or full-level competence.
- Hardened evaluator/schema trust surfaces and fixed the first-visible close-to-fire bridge. PPO checkpoint loading now rejects reordered or renamed existing actions instead of trusting action dimensions alone, while eval checkpoint loading targets the current observation/action schema so appended features/actions migrate consistently. Forced-option eval now separates forced-skill disallowance from selected-action invalidity, and eval aggregates now report earned item/secret metrics plus reset-source breakdowns. Bad milestone: the first live `forced-first-visible-close-fire-killterm-next` run still kept `close_visible_contact` mask-allowed through max steps, never handed off to fire, and earned `kill_delta=0`. Fix: when `fire` is legal, the skill mask now returns fire as the hard priority instead of re-adding stale recent-contact closure. Good live milestone: rerun `forced-first-visible-close-fire-killterm-yield-fix` restored `0500-first-visible_snapshot`, reached first-shootable contact, applied 47 legal fire handoffs, earned `kill_delta=1` with `done_reason=required_kills`, and had `invalid_action_steps=0`, `selected_disallowed_steps=0`, `strict_allowed_skill_fallback_steps=0`, and `snapshot_verification_failures=0`. This is control/eval hygiene and a restored bottleneck bridge pass, not full-level PPO competence evidence.
- Ran the fixed first-visible and mixed three-stage PPO gates after the fire-priority mask fix, using a temporary memory copy so the experiment did not mutate committed training memory. Fixed run `ppo-first-visible-v2-speed-wave3-20260627-a` produced four checkpoints; every checkpoint-eval episode across `first-visible`, `first-enemy-shootable`, and `first-damage` earned `kill_delta=1`, ended with `done_reason=required_kills`, and had 0 invalid actions, 0 strict allowed-skill fallbacks, and 0 snapshot verification failures. Best fixed checkpoint was update 2 (`selection_score=337.9208`, worst stage `323.6875`), with first-visible required-kill steps `[137, 222, 222]`, enemy-shootable `[53, 53, 53]`, and first-damage `[9, 9, 9]`. Mixed run `ppo-mixed-stage-wave3-20260627-a` resumed that checkpoint and covered all three native slots in one 384-record buffer (`first-visible=225`, `first-enemy-shootable=141`, `first-damage=18`), restored 8 snapshots with 0 verification failures, used only `close_visible_contact` and `fire`, reached `shootable_target_steps=159`, `fire_on_shootable_steps=159`, `damage_delta=45`, and earned `kill_delta=4`; checkpoint eval again earned required kills in all 9 episodes with 0 invalid/fallback/verification failures. This strengthens restored bottleneck PPO competence and selection stability; it still does not prove full true-spawn level completion.
- Ran a 20-subagent review/diagnostic wave in batches because the runtime caps active subagents. Good milestone: the subagents independently converged on the same state of the project: restored first-visible PPO competence is real, true-spawn/full-level PPO competence is not proven, native save-slot portability remains an export gap, and the next full-level bottleneck is post-combat navigation/exit behavior rather than raw combat. Live true-spawn eval `ppo-true-spawn-transfer-wave3-20260627-a` from `ppo-mixed-stage-wave3-20260627-a-ppo-0000.pt` earned `kill_delta=2` and `item_delta=3` with 0 invalid actions, proving the snapshot-trained bridge can transfer some combat behavior to fresh E1M1. Bad milestone: it then froze at one post-combat coordinate with only `route_progression` and `recover_stuck` available; `recover_stuck` repeatedly emitted `unstick_turn` with `stuck_phase=2`.
- Fixed that post-combat recovery bug. `unstick_turn` no longer counts as a generic non-locomotion decision for stuck detection, so the recovery timer is not reset after every turn-only recovery action and the routine can advance to later recovery phases. Focused tests passed, then the full agent test suite passed (`221 passed`). Rerun `ppo-true-spawn-transfer-wave3-20260627-b` with the same checkpoint no longer pinned to one coordinate: candidate survived to the 5k-tic cap with `kill_delta=2`, `item_delta=2`, 0 invalid actions, `first_visible_contacts=1`, `first_shootable_contacts=1`, and nonzero recent movement. Bad milestone: it still failed promotion (`level_completed=false`, `mean_reward=38.849`, `mean_stuck_events=183`) while the heuristic baseline completed E1M1 in the same eval with `level_transition_delta=1`, `max_kill_gain=5`, and `max_item_gain=4`.
- Tested naive full-run behavior-cloning warm start from `trajectories/brain-train-68-current-success.jsonl`. The trajectory mapped cleanly into PPO skills (`route_progression=814`, `seek_enemy=550`, `open_use_line=483`, `fire=467`, `recover_stuck=339`, `engage=192`, `press_exit=119`) and one PPO update `ppo-full-bc-true-spawn-wave3-20260627-a` proved the policy can select missing door/use skills (`open_use_line=109`) while still earning `kill_delta=2` in rollout. Bad milestone: direct eval of the resulting checkpoint regressed badly (`ppo-full-bc-true-spawn-eval-wave3-20260627-a`: `kill_delta=0`, `item_delta=1`, `shootable_target_steps=0`, `total_reward=-18.6519`). Conclusion: full-run BC alone dilutes the restored combat bridge; the next candidate should start from the mixed snapshot combat checkpoint and add targeted door/exit/post-combat curriculum rather than replacing it with naive imitation.
- Tried to recapture native post-combat and post-combat-exit-route slots after the recovery fix. Bad milestone: first capture attempt `snapshot-capture-postcombat-wave3-20260627-a` did not reach the `post_combat_kills=5` gate (`max_kills=2`, `max_items=2`) and exposed another deterministic route issue: while stuck at walk-trigger line 195, the controller kept emitting `cross_progression_line` because walk-trigger routing bypassed blocked-front recovery. Fixed that route branch so a stuck, forward-blocked walk-trigger route yields to `_recover_from_stuck()` before trying to cross. Full tests passed again (`222 passed`). Rerun `snapshot-capture-postcombat-wave3-20260627-b` no longer pinned to one coordinate, but still failed to match requested milestones by the 12k-state cap (`max_kills=2`, `max_items=1`). Current conclusion: route recovery improved, but current structured-brain capture still cannot reliably produce fresh 5-kill post-combat native slots; the next training path needs either lower-threshold staged post-combat slots first or a targeted exit-route capture from an already successful trajectory/snapshot.
- Captured a lower-threshold native post-combat slot as a staged routing rung. `snapshot-capture-postcombat2-wave3-20260627-a` matched `post-combat` at 2 kills, wrote native save slot 5, and verified load restoration with `matched=true` (`kills=2`, `items=2`, `health=102`, route waypoint line 195). Validating the manifest is clean aside from the expected missing external `.snap` artifact warning for native save-slot-only stages. Bad milestone: evaluating `ppo-mixed-stage-wave3-20260627-a-ppo-0000.pt` from that verified slot (`eval-postcombat2-mixed-wave3-20260627-a`) did not complete the level or earn more kills: `done_reason=max_steps`, `level_completed=false`, `kill_delta=0`, `item_delta=1`, `skill_counts={route_progression:770, open_use_line:614, recover_stuck:12}`, `snapshot_verification_failures=0`, and selection score `-9.0907`. This gives us a real restored post-combat routing gate, but the current PPO checkpoint fails it.
- Ran a short PPO continuation on the verified 2-kill post-combat slot: `ppo-postcombat2-route-wave3-20260627-a` resumed `ppo-mixed-stage-wave3-20260627-a-ppo-0000.pt` for two 512-record updates with checkpoint eval after each update. Good milestone: best checkpoint `ppo-postcombat2-route-wave3-20260627-a-ppo-0001.pt` evaluated from slot 5 with `kill_delta=4`, `item_delta=2`, `max_kills=6`, `max_items=4`, `damage_delta=115`, `shootable_target_steps=256`, `fire_on_shootable_steps=250`, `skill_counts={close_visible_contact:486, fire:250, open_use_line:174, press_exit:24, route_progression:477, seek_enemy:17}`, 0 invalid actions, 0 stuck events, 0 snapshot verification failures, and `survival_rate=1.0`. Bad milestone: it still timed out with `level_completed=false`, `level_transition_delta=0`, and `steps_to_exit=4000`, so this is post-combat combat/recovery improvement, not exit-routing competence. Also note the eval/rollout mismatch: update 1's collected rollout had no new kills, while deterministic checkpoint eval did; use checkpoint eval as the gate and do not overread single rollout buffers.
- Fixed the post-combat checkpoint-selection leak. Score schema `restfuldoom.ppo_checkpoint_eval_score.v4` now scores stages labeled `post-combat` or `post-combat-exit-route` as exit-routing objectives even before completion, with shaped reward capped and earned-kill bonus ignored unless the stage actually exits; `required_kill_speed` still takes priority for kill-terminated bottleneck episodes. Full Python agent tests passed (`226 passed`). Live score-fix eval `eval-postcombat2-scorefix-wave3-20260627-a` restored native save slot 5 with 0 snapshot verification failures and reclassified the same post-combat checkpoint as `post_combat_exit_routing`: `selection_score=36.0`, `earned_kill_bonus=0.0`, `exit_success_rate=0.0`, `reward_tiebreak=20.0`, `stuck_penalty=-4.0`. Bad milestone: the checkpoint still did not complete the level (`done_reason=max_steps`, `level_completed=false`, `level_transition_delta=0`, `steps_to_exit=4000`) and only earned a small post-restore combat/item gain (`kill_delta=1`, `item_delta=1`), so the next learning pass needs exit-route reward/targeting rather than more kill credit.
- Added dense exit-route shaping for the next post-combat training pass. `DoomEnvConfig` now has `exit_route_progress_reward`, `exit_route_reached_reward`, and `exit_route_failure_penalty`; when protobuf marks a route waypoint as `exit`, route-progression reward gets extra progress/reach credit and extra failure penalty. PPO CLI flags and checkpoint reward metadata carry the same knobs. Focused tests prove normal route reward is unchanged while exit-marked route reward is boosted, and the full Python agent suite passed (`227 passed`). Bad/limited milestone: this is learning-signal plumbing only; no new PPO run has yet shown that the postcombat2 checkpoint can convert the restored slot into a level transition.
- Ran the postcombat2 exit-shaping continuation `ppo-postcombat2-exitshape-wave3-20260627-a` from the verified 2-kill native slot. Bad diagnostic milestone: both updates had `exit_route_attempt_steps=0`, `exit_route_progress_units=0`, `exit_route_reached_steps=0`, and `exit_route_failed_steps=0`; generic route activity happened, but the selected target stayed non-exit, so the new exit-route shaping never activated. Limited good signal: best eval checkpoint still earned post-restore combat gains (`kill_delta=3`, `item_delta=1`, `damage_delta=80`, `selection_score=40.0`) with 0 invalid actions and 0 snapshot verification failures, but it timed out without a level transition. Follow-up patch tightened the control/eval surface rather than claiming new learning: the structured brain now prefers visible far exit lines after post-combat kills when combat is quiet, recent-contact route-failure suppression no longer suppresses exit routes, route outcome accounting follows the controller-selected line and records `target_source`, checkpoint eval serializes/aggregates route and exit-route diagnostics, and `post-combat-exit-route` snapshots require actual exit route-waypoint evidence. Live patched eval `eval-postcombat2-targeting-patch-20260628-a` remained a bad milestone: it restored slot 5 with 0 verification failures, earned combat gains (`kill_delta=3`, `item_delta=1`, `damage_delta=115`) but died (`done_reason=death`, `min_health=-15`) before exit with `level_completed=false`, `level_transition_delta=0`, `selection_score=3.6857`, `route_attempt_steps=180`, and still `exit_route_attempt_steps=0`. This proves the eval diagnostics are now visible, but the existing checkpoint has not learned the post-combat exit route.
- Added checkpoint-curriculum eval trace files and tightened the `press_exit` action contract after trace review. Pre-patch trace `eval-postcombat2-trace-20260628-a` showed the restored postcombat2 checkpoint still failed the exit gate (`done_reason=max_steps`, `level_completed=false`, `kill_delta=3`, `damage_delta=135`, `route_attempt_steps=222`, `exit_route_attempt_steps=59`, `exit_route_reached_steps=0`, `exit_route_progress_units=706.3174`, `snapshot_verification_failures=0`), and the 12 `press_exit` selections were still roughly 813-883 units from exit line 330. The patch makes checkpoint eval traces stage/update-specific, keeps far exit lines under `route_progression`, and exposes `press_exit` only when the exit option is inside the real activation/push window. Post-patch sanity eval `eval-postcombat2-pressmask-20260628-a` removed far `press_exit` choices (`skill_counts` had no `press_exit`) and attributed line 330 to exit-route progression, but it still died without a level transition (`done_reason=death`, `kill_delta=2`, `damage_delta=80`, `exit_route_attempt_steps=24`, `exit_route_progress_units=10.8184`). This is trace/debugging and control-label hardening, not a learning breakthrough.
- Added exit-aware post-combat recovery after a true-spawn diagnostic trace narrowed the route failure. Baseline trace `eval-current-true-spawn-route-trace-20260628-a` survived to max steps, earned `kill_delta=2`, saw exit line 330 (`exit_route_attempt_steps=361`, `exit_route_progress_units=305.6`), but never reached the exit and looped between exit line 330 and stale walk-trigger/route waypoint line 195 (`route_failed_steps=923`, `exit_route_reached_steps=0`). A read-only Claude/tmux review recommended deterministic final-exit hardening before more PPO shaping. Patch: stuck recovery now routes toward visible exit lines in quiet post-kill states, and a short post-combat exit-line memory suppresses fallback to stale walk-trigger routes after an actual exit-line action while that exit line remains visible. Focused tests and the full agent test suite passed (`240 passed`). Bad/limited milestone: the half-patched live eval `eval-current-true-spawn-exitrecover-20260628-a` earned 5 kills but died before exit-route attempts (`done_reason=death`, `exit_route_attempt_steps=0`), so this commit is route/recovery control hardening, not proven level-completion improvement or new PPO competence.
- Ran the first formal restored postcombat2 Gate A preflight after exit-recovery hardening. Three checkpoint-eval episodes from native save slot 5 restored cleanly (`snapshot_verification_failures=0`) but failed the gate: `level_completion_rate=0.0`, `exit_route_attempt_steps=1092`, `exit_route_failed_steps=1053`, `exit_route_reached_steps=0`, `survival_rate=0.6667`, and one death. Trace review showed the same stale-waypoint shape: PPO kept selecting `route_progression` toward exit line 330 while the live route waypoint stayed on walk-trigger line 195, and `turn_from_block` prevented ordinary stuck detection from taking over. Patch: after repeated failed exit-route attempts where the chosen progression line is an exit but the protobuf route waypoint is a different walk trigger, the action mask now exposes `recover_stuck` and suppresses `route_progression`; direct `route_progression` execution also falls back to the exit-recovery path. Full tests passed (`242 passed`). Good short live recheck `ppo-postcombat2-exitrecover-preflight-20260628-b`: one restored-slot episode survived, improved to `exit_route_failed_steps=0`, had `exit_route_progress_units=69.6844`, earned `kill_delta=2` and `item_delta=1`, and kept `snapshot_verification_failures=0`. Bad milestone: it still timed out with `level_completed=false`, `exit_route_reached_steps=0`, so this is controller-surface improvement, not learned post-combat exit competence.
- Bounded follow-up training probe `ppo-postcombat2-exitrecover-train-20260628-a` did not solve Gate A. Two 512-record PPO updates from the verified postcombat2 native slot restored cleanly (`snapshot_verification_failures=0`) and update 0 earned `kill_delta=1`, but checkpoint eval still had `level_completion_rate=0.0`, `exit_route_attempt_steps=0`, `exit_route_reached_steps=0`, and route failure pressure (`route_failed_steps=169/300`). Trace review narrowed the issue: when the live route waypoint stayed on far walk-trigger line 195, the policy could keep selecting `route_progression` even while nearby manual use-lines 151/152 were available, so exit-route shaping never activated.
- Added stale far-walk-route recovery for post-combat/no-enemy states. After repeated no-progress route attempts toward a distant walk-trigger waypoint, the PPO mask now suppresses `route_progression` and exposes `recover_stuck` when a local use-line affordance is available; direct `route_progression` also falls into recovery instead of bypassing the mask. Full tests passed (`243 passed`). Live restored-slot recheck `ppo-postcombat-staleroute-recheck-20260628-a` reduced route failure pressure (`route_failed_steps=67` over 285 records versus hundreds in the failed probe), kept `survival_rate=1.0`, and restored with `snapshot_verification_failures=0`. Bad milestone: it still timed out with `level_completion_rate=0.0`, `exit_route_attempt_steps=0`, and `exit_route_reached_steps=0`, so this is another control-surface cleanup, not learned exit competence.
- Fixed the postcombat2 snapshot routing context that was hiding the exit signal. In native snapshot resets, `start_kills` equals the inherited restored kill count, so the controller's "earned a kill this episode" guard prevented post-combat exit routing from activating even though the map state already had 2 kills. The controller now treats absolute `kills>=2` as post-combat context for routing only, while reward/eval still use earned deltas. It also widens far visible exit-line selection/recovery to 3,200 units and counts exit-directed `recover_stuck`/`press_exit` decisions as exit-route attempts only when the selected line is an actual exit line. Full tests passed (`247 passed`).
- Live validation after that patch showed a real signal unlock but not a solved gate. Forced `route_progression` from native slot 5 now targets exit line 330 instead of stale route waypoint 195 (`exit_route_attempt_steps=60`, `exit_route_progress_units=474.0603`, `snapshot_verification_failures=0`). Checkpoint eval of the existing postcombat2 model then exposed exit-directed recovery in metrics (`exit_route_attempt_steps=262`, `exit_route_progress_units=552.5668`, `survival_rate=1.0`) but still had `level_completion_rate=0.0` and `exit_route_reached_steps=0`. Bounded PPO continuation `ppo-postcombat2-farexit-train-20260628-a` improved the checkpoint eval score from `-692.5157` to `-509.807`, increased eval exit-route progress to `747.0801`, reduced eval failed exit-route steps from `314` to `183`, and kept `snapshot_verification_failures=0`; bad milestone: it still did not reach/complete the exit. Closest traced exit distance was about 2,058 units, with the Doom route waypoint still on walk-trigger line 195, so the next rung is physical route completion around line 195 / exit approach rather than combat or reward visibility.

## Open Risks

- Repeatability still needs improvement; known structured-brain runs can complete E1M1, but recent PPO/current-code diagnostic runs do not reliably complete it and final door/exit sequencing remains the highest-risk segment.
- PPO episode reset can now request seed values, but Doom RNG seeding has not been safely wired yet; training/eval gates should treat seeds as labels until `seed_applied=true` is implemented and verified.
- Live PPO checkpointing, from-scratch PPO combat kills, and combat-curriculum eval promotion are demonstrated in scoped gates. Full independent level competence is not. The next proof point is transferring the PPO checkpoint back to spawn/navigation curriculum and challenging the deterministic full-level baseline through the promotion gate.
- Current bottleneck: PPO has scoped combat and restored-bottleneck competence, but full true-spawn level completion remains blocked by post-combat survival, route recovery, and exit-line targeting/activation. The latest evidence does not yet show learned post-combat exit routing or reliable level transition.
- Observation richness is still a full-level PPO risk: the current vector now has structured combat/navigation, sector hazard, route-waypoint affordances, one-step route-outcome history, bounded temporal trend features, topology frontier count, local topology-context features, visible-contact geometry, optional visible-contact progress reward, graded contact-ray execution, short-lived contact use-line memory, and explicit contact-context features, but no compact topology graph, projectile threat, progressed-map snapshot state, or recurrent neural state.
- Fresh-reset curriculum cannot honestly replay mid-trajectory states. Route/contact curriculum from successful rollouts needs save-state or Hellbox/Shrink snapshot restore; otherwise PPO is learning from teleport starts whose map state does not match the original trajectory.
- First-contact curriculum is a training aid only. A checkpoint that reaches first visible/shootable contact is not promotable unless it later satisfies the full promotion gate: completion, kills, survival, time, and stuck-event metrics against the deterministic baseline.
- Pistol firing cadence may need raw ticcmd or carefully timed shoot pulses if high-level `ACTION_SHOOT` is too lossy.
- The learned skill selector is behavior cloning, not independent RL yet. It predicts skill choices from successful protobuf trajectories and is exported for cloud resume, but the deterministic controller still owns live tic-level actions.
- Training-job export is closer to cloud-portable but native `save_slot:N` curricula still need the actual `agentdoomN.dsg` files plus checksums/install metadata before Docker-local snapshot curricula can resume honestly in a cloud runner.
- Naive full-run BC is not a promotion path by itself. It teaches door/use-line actions but can suppress the reliable snapshot-trained combat bridge, so future full-level work should use targeted post-combat/exit snapshots or staged distillation rather than one broad imitation pass.
- Post-combat native slot capture remains blocked at the 5-kill gate in the current Docker run even after route recovery fixes. Treat any generated post-combat manifest as invalid unless it reports matched selectors and verified native save-slot loads; for `post-combat-exit-route`, require `route_waypoint_exit=true` plus positive route line evidence, not just exit-control skill/use-line evidence.
- A verified 2-kill post-combat native slot exists locally in save slot 5, but it is a rung, not a success criterion. The current mixed PPO checkpoint fails to exit from it, so future PPO work should train/evaluate this stage explicitly before retrying full true-spawn promotion.
- The first postcombat2 PPO continuation learned to extend combat from the restored slot but still did not learn exit routing. The checkpoint scorer now treats post-combat stages as exit-routing gates, and route-progress shaping can now favor exit-marked waypoints, but the next proof still requires a live post-combat PPO run that turns that signal into an actual level transition.
